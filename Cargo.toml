[workspace]
members = [
    "libprotonup",
    "protonup",
    "protonup-gui",
    "protonup-rs",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
debug = 0
rpath = false
panic = "unwind"
incremental = false
strip = "debuginfo"
